{{ outlet }}

{{#if composer.modalOpen}}
  {{#modal-dialog
    attachment='top right'
    close=(route-action "toggleCompose")
  }}
    {{ compose-form sendMail=(route-action 'sendMail') close=(route-action "toggleCompose")}}
  {{/modal-dialog}}
{{/if}}
