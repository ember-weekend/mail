{{ outlet }}

{{#if composer.modalOpen}}
  {{#modal-dialog
    attachment='top right'
    close=(route-action "toggleCompose")
  }}
    {{ compose-form sendEmail=(route-action 'sendEmail') close=(route-action "toggleCompose")}}
  {{/modal-dialog}}
{{/if}}
